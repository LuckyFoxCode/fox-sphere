node_modules
**/node_modules
dist
**/dist
.prisma
**/src/generated

# Secrets — never bake into image layers
.env
.env.*
**/.env
**/.env.*

# Local runtime data (dev postgres bind-mount — root-owned, never in image)
data

# VCS / tooling / local
.git
.gitignore
.vscode
.idea
*.log
.DS_Store

# Local-only apps - never part of any deployed image (see AGENTS.md architecture table).
# Keeps them out of the build context entirely, not just out of the final stage.
apps/admin
apps/api

# Docker meta
Dockerfile
docker-compose*.yml
.dockerignore
