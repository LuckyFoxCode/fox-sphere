{
  "extends": "./tsconfig.app.json",

  // tsconfig.app.json excludes __tests__ so app builds stay clean; this project is
  // what type-checks the test files themselves.
  "include": ["src/**/__tests__/*", "env.d.ts"],
  "exclude": [],

  "compilerOptions": {
    "lib": [],
    "types": ["node"],
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.vitest.tsbuildinfo"
  }
}
